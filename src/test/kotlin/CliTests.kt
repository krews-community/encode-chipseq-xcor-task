import org.junit.jupiter.api.Test
import java.nio.file.Files
import testutil.*

class CliTests {

    @Test
    fun `Test cli`() {
     /*   val args = """
            -bam=$BAM
            -outputDir=${testOutputDir.resolve("filteroutput")}
            -outputPrefix=testprefix
            -pairedEnd
        """.trimIndent().split("\\s+".toRegex())
        main(args.toTypedArray())*/
    }
}